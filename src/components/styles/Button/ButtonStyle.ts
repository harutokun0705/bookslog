import { makeStyles } from '@material-ui/core/styles';
export const ButtonStyles = makeStyles({
    button: {
        backgroundColor: '#2d76ca',
        "&:hover": {
            backgroundColor: '#8eabcc',
        },
    },
    textColor: {
        color: '#fff'
    }
});